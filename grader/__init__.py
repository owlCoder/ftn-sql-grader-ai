"""
grader package
--------------
ODP SQL Grader — modular SQL assignment grading tool.

Sub-modules:
  config     — configuration loading and typed AppConfig wrapper
  logger     — logging setup and in-memory ListHandler
  models     — TeamRepo data class
  sheets     — Google Sheets CSV fetching and team parsing
  repository — repo name validation, git cloning, SQL file discovery
  grader     — LM Studio API calls and grading logic
  report     — HTML report generation
"""
